for (var table = 1; table <= 10; table++) {
    console.log('Tafel van ' + table + ' ---------');
    for (var num = 1; num <= 10; num++) {
        console.log(num + ' * ' + table + ' = ' + num * table);
        //console.log(`${num} * ${table} = ${num * table}`); string met back tick
    }
}
